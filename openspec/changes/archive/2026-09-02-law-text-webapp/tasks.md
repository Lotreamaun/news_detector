## 1. Конфигурация и зависимости

- [x] 1.1 Добавить aiohttp в зависимости (requirements.txt / pyproject, как принято в проекте)
- [x] 1.2 Конфиг: поля `WEBAPP_HOST` (дефолт 127.0.0.1), `WEBAPP_PORT` (дефолт 8080), `WEBAPP_URL` (по умолчанию пусто = фича выключена); загрузка в `Config.load()` + валидация `WEBAPP_PORT > 0`

## 2. Бэкенд WebApp-сервера (пакет app/webapp)

- [x] 2.1 Создать `app/webapp/__init__.py` с функцией запуска/остановки сервера (aiohttp, стартует в event loop бота)
- [x] 2.2 Реализовать API-роут `GET /full_text?external_id=...` — отдаёт JSON `{title, url, text, is_text_available}` из `original_text` (async сессия БД); пустой текст → `is_text_available=false`
- [x] 2.3 Реализовать роут `GET /app?external_id=...` — отдаёт HTML-страницу фронтенда (читать external_id из query и подставлять в страницу)

## 3. Фронтенд Mini-App

- [x] 3.1 HTML-страница: грузит текст по `/full_text?external_id=...`, отображает заголовок, сам текст, ссылку на оригинал
- [x] 3.2 Пометка «распознано с помощью ИИ» (дисклеймер) поверх текста
- [x] 3.3 Состояние «текст недоступен»: сообщение + ссылка на оригинал (is_text_available=false)
- [x] 3.4 Читаемая вёрстка (адаптивная, скролл для длинного текста, мобильный вид под Telegram WebApp)

## 4. Интеграция с ботом

- [x] 4.1 В `app/main.py`: старт WebApp-сервера в `post_init`, остановка в `post_shutdown`; при ошибке старта — предупреждение в лог и продолжение работы бота
- [x] 4.2 Хелпер построения кнопки «Полный текст» (WebAppInfo) по `WEBAPP_URL` + `external_id`; возвращает None при пустом/неверном `WEBAPP_URL`
- [x] 4.3 Добавить кнопку «Полный текст» в `_deliver` (все места доставки саммари: команда `/summary`, кнопка force-summarize) и в `_build_notification` — поверх существующих кнопок, ссылка на оригинал в тексте сохраняется

## 5. Документация

- [x] 5.1 `.env.example`: блок `WEBAPP_HOST` / `WEBAPP_PORT` / `WEBAPP_URL` с пояснением про обязательный HTTPS для публичной ссылки
- [x] 5.2 README: раздел про WebApp (запуск, хостинг по HTTPS, поведение при пустом `WEBAPP_URL`)

## 6. Проверка

- [x] 6.1 py_compile изменённых модулей (config, main, app/webapp, handlers, scheduler)
- [x] 6.2 Логическая проверка сценариев specs/content-viewing (кнопка в сообщении и уведомлении, наличие/отсутствие текста, дисклеймер, пустой WEBAPP_URL)

## 7. Доработка отображения текста (UX-фикс)

- [x] 7.1 Фильтр хвоста подписи: удалить «подписано эл. подписью» + ФИО/сертификат эл. подписи, оставить суть + дату; дату подписания показать сверху после заголовка (если парсится, иначе без неё)
- [x] 7.2 Убрать блок-код/max-height у текста, сделать обычный текст после заголовка с единым скроллом страницы (без вложенного скролла контейнера)
- [x] 7.3 Таблицы Markdown `|...|` → HTML `<table>` (доработать `isTableSeparator`), остальной Markdown в обычном тексте — стрипать (`stripPlainMarkdown`)
- [x] 7.4 Фолбэк для непредвиденных форматов: показать экранированный текст как есть + ссылку на оригинал, без падения
- [x] 7.5 Визуальная проверка WebApp (дата сверху, таблицы, фолбэк, единый скролл, без блока-кода)
