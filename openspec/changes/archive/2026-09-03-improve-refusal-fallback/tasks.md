## 1. Детекция отказа (гибридная эвристика)

- [x] 1.1 В `app/services/gigachat.py` заменить `looks_like_refusal` на `classify_reply(text) -> Literal["content","refusal","empty"]`: структурированные группы точных маркеров (RU+EN) + лёгкие regex-паттерны на перефразировки; `empty` для пустых ответов
- [x] 1.2 Добавить двухуровневый порог по D2: короткие ответы решаются по любому маркеру/паттерну, длинные — только по «сильным» маркерным фразам (буклеты вида «не обладает собственным мнением», «сгенерирован нейросетевой моделью»)
- [x] 1.3 Сделать `looks_like_refusal(text)` тонкой обёрткой над `classify_reply` (сохранение подписи/вызывающих кодов)

## 2. Подключение во всех точках генерации

- [x] 2.1 `app/services/summarizer.py`: при `classify_reply == "refusal"` бросать `GigaChatError` (единый обработчик); при `"empty"` — тоже не принимать как саммари
- [x] 2.2 `app/services/rss_parser.py` (ocr_document_text): при `"refusal"`/`"empty"` возвращать `None` вместо текста дисклеймера
- [x] 2.3 Проверить, что распознанный отказ не просачивается в `article.summary` / `article.original_text` ни в одном пути (daily, button, `/summary`, OCR)

## 3. Уведомление-фолбэк

- [x] 3.1 Фоновая daily: подтвердить, что отказ → `summary=None` → штатный фолбэк `_build_notification` со ссылкой на оригинал (без текста дисклеймера)
- [x] 3.2 Пути принудительной саммаризации (`handlers.py`): фолбэк об отказе встраивается в тело уведомления вместо саммари (название + ссылка на портал + кнопка «Полный текст» WebApp остаются) — не отдельным сообщением; в тексте — «Языковая модель ...», без ссылки на провайдера

## 4. Проверка и регрессия

- [x] 4.1 Собрать тестовую матрицу: негативный набор реальных отказов GigaChat (включая две попавшие при тестах) + негативный набор настоящих саммари/OCR
- [x] 4.2 Покрыть детекцию: подтвердить попадание ≥70–80% отказов и отсутствие ложных срабатываний на матрице (песочница/unit)
- [x] 4.3 `py_compile` всех изменённых файлов и `openspec validate --changes`
- [x] 4.4 Ручная проверка на проде: отказ по кнопке/`/summary` показывает фолбэк-сообщение, а не лонгрид; `/latest` без следов отказа
