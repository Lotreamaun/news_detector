## 1. Рефакторинг форс-саммаризации

- [x] 1.1 Вынести общее тело `force_summarize` (проверка лимита → получение текста → OCR → саммари → обновление Article → инкремент → отправка) в переиспользуемую async-функцию `_summarize_and_reply(context, user, external_id)` в `app/bot/handlers.py`
- [x] 1.2 Переписать `force_summarize` как тонкую обёртку над `_summarize_and_reply`, сохранив прежнее поведение (парсинг callback_data, регистрация пользователя, answer_callback_query)

## 2. Команда /notify_me

- [x] 2.1 Добавить `notify_me` в `app/bot/handlers.py`: аргумент external_id, поиск Article в БД, при нахождении — отправка `_build_notification` (текст+кнопка) вызывающему пользователю; «закон не найден» и подсказка при отсутствии аргумента

## 3. Команда /summary

- [x] 3.1 Добавить `summary_command` в `app/bot/handlers.py`: аргумент external_id, регистрация пользователя, вызов `_summarize_and_reply` (общий месячный лимит с кнопкой)

## 4. Регистрация и справка

- [x] 4.1 Добавить `CommandHandler("notify_me", …)` и `CommandHandler("summary", …)` в `_build_application` (`app/main.py`)
- [x] 4.2 Добавить команды в `HELP_TEXT` (`app/bot/handlers.py`) и в `set_my_commands` (`app/main.py`)
- [x] 4.3 py_compile изменённых модулей (handlers, main)
- [x] 4.4 Логическая проверка сценариев из specs/manual-summary-request (закон найден/не найден, успех, текст недоступен, лимит, общий лимит)

## 5. Индикация и результат в том же сообщении (кнопка)

- [x] 5.1 Показать обратную связь «Саммари в процессе создания…» при нажатии кнопки (правка исходного сообщения / индикатор), пока идёт генерация
- [x] 5.2 Выводить результат в том же сообщении и убирать кнопку: изменить `_summarize_and_reply`/`force_summarize`, чтобы для кнопки использовался `edit_message_text` исходного сообщения (индикатор → финальный саммари без кнопки), а не новое сообщение

## 6. /summary по ссылке

- [x] 6.1 Добавить хелпер `_resolve_external_id(value)` в `app/bot/handlers.py`: извлечение `external_id` из голого id или из ссылки правового портала
- [x] 6.2 Применять хелпер в `summary_command` (приём ссылки или id), опционально в `notify_me`

## 7. Переиспользование сохранённого саммари

- [x] 7.1 В начале `_summarize_and_reply` загружать `Article` по `external_id`; если `article.summary` уже есть — доставить сохранённое саммари через `_deliver` (для кнопки — правка исходного сообщения, для команды — новое) и завершить без вызова LLM
- [x] 7.2 Не инкрементировать счётчик `SummarizationUsage` при переиспользовании сохранённого саммари (инкремент остаётся только при фактической новой LLM-генерации)
- [x] 7.3 py_compile handlers.py и логическая проверка сценария «Саммари уже сохранено» (кнопка и /summary не обращаются к LLM повторно)
