## 1. Приветственный экран

- [x] 1.1 В `start()` (`app/bot/handlers.py`) для ветки нового пользователя заменить текст на короткое приветствие без списка команд
- [x] 1.2 Заменить клавиатуру с 3 кнопок на одну — «Как это работает?» (новый `callback_data`, например `onboarding:example`)
- [x] 1.3 Убедиться, что ветка для уже зарегистрированного пользователя (`is_new=False`) не изменилась

## 2. Демонстрация пользы

- [x] 2.1 Добавить `CallbackQueryHandler` на `onboarding:example`, который вызывает существующую логику `show_example` (или напрямую переиспользует функцию) для отправки тестового уведомления
- [x] 2.2 После отправки тестового уведомления показать следующим сообщением экран с предложением подписаться на канал и кнопкой «Проверить подписку» (`callback_data="check_subscription"`, определена в `channel-subscription-gate`)

## 3. Раскрытие мастера фильтров после гейта

- [x] 3.1 В обработчике успешной проверки подписки (`channel-subscription-gate`, задача 4.2) после установки `channel_verified=True` показать экран с кнопками `Настроить` (`wizard:setup`) / `Пропустить → Все` (`wizard:skip`) — теми же, что и сейчас
- [x] 3.2 Убедиться, что `level_wizard_handler.entry_points` (regex `^wizard:(setup|skip)$`) не требует изменений — новый экран использует тот же `callback_data`

## 4. Регистрация хендлеров

- [x] 4.1 Зарегистрировать новый `CallbackQueryHandler` (`onboarding:example`) в `app/main.py` (`_build_application`), рядом с существующим `show_example`

## 5. Проверка

- [x] 5.1 Вручную прогнать полный сценарий новым `telegram_id`: `/start` → «Как это работает?» → тестовое уведомление → «Проверить подписку» (без подписки) → сообщение об отсутствии подписки → подписаться → «Проверить подписку» → экран мастера фильтров → `Настроить`/`Пропустить`
- [x] 5.2 Убедиться, что `/start` для уже существующего пользователя выглядит как раньше (не переехал на новый флоу)
- [x] 5.3 Убедиться, что `/settings` для существующего пользователя работает как раньше, без изменений
