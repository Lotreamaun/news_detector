## 1. Разделение обработки и рассылки

- [ ] 1.1 В `check_legislation_updates` (`app/services/scheduler.py`) убрать вызов рассылки из `_process_entry`; цикл по `new_entries` только обрабатывает и сохраняет документы, собирая список успешно сохранённых `Article`
- [ ] 1.2 После цикла обработки вызвать новую функцию рассылки один раз со всем списком статей цикла

## 2. Группировка по пользователю

- [ ] 2.1 Реализовать `_notify_users_batch(context, session_maker, articles: list[Article])`: выбрать активных (и, после `channel-subscription-gate`, подтверждённых) пользователей одним запросом
- [ ] 2.2 Для каждого пользователя отфильтровать `articles` по его `UserFilter`, переиспользуя существующую логику сопоставления фильтра из текущей `_notify_users`
- [ ] 2.3 Если подходящих статей 1 — использовать существующий `_build_notification` (без изменений)
- [ ] 2.4 Если подходящих статей 2+ — вызвать новую функцию построения дайджеста

## 3. Форматирование дайджеста

- [ ] 3.1 Реализовать функцию построения текста дайджеста: пронумерованный список статей (заголовок + краткое саммари на каждую), без кнопок на пункт, по паттерну `_format_summary`/`_full_text_block`
- [ ] 3.2 Добавить накопление длины текста с порогом (с запасом от лимита Telegram 4096) и обрезку списка с пометкой `"...и ещё N законов, смотрите /latest"` при превышении

## 4. Сохранение существующей защиты при отправке

- [ ] 4.1 Перенести в `_notify_users_batch` существующую логику паузы `asyncio.sleep(0.05)` между пользователями
- [ ] 4.2 Перенести retry-логику на 429 (`retry_after`) и деактивацию (`is_active=False`) при 403/Forbidden — применяется к отправке сообщения пользователю независимо от того, индивидуальное оно или дайджест

## 5. Проверка

- [ ] 5.1 Вручную (или через временный скрипт) смоделировать цикл с 1 новым документом, подходящим под фильтр тестового пользователя — убедиться, что приходит обычное индивидуальное сообщение с кнопками
- [ ] 5.2 Смоделировать цикл с 3 новыми документами, все подходят под фильтр — убедиться, что приходит один дайджест с 3 пунктами
- [ ] 5.3 Смоделировать цикл, где документы подходят под фильтр одного пользователя, но не подходят под фильтр другого — убедиться, что у каждого свой независимый результат (индивидуальное/дайджест/ничего)
- [ ] 5.4 Проверить, что при очень длинном списке (много документов с длинными саммари) дайджест обрезается с пометкой "...и ещё N" вместо ошибки отправки от Telegram
- [ ] 5.5 Убедиться, что `/latest` не изменился в поведении
