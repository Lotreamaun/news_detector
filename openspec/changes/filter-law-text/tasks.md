## 1. Фильтрация шапки и хвоста в WebApp

- [ ] 1.1 Расширить JS-фильтр в `app/webapp/server.py` (`filterSignatureTail` → `filterLawText`): удалять шапку в начале текста — `ПОСТАНОВЛЕНИЕ ПРАВИТЕЛЬСТВА РОССИЙСКОЙ ФЕДЕРАЦИИ`, повтор `ПОСТАНОВЛЕНИЕ от 31.08.2026`, `МОСКВА`/`г. Москва` — только первые строки/15%, консервативно
- [ ] 1.2 Расширить хвост-фильтр на верхний регистр `ПОДПИСАНО ЭЛЕКТРОННОЙ ПОДПИСЬЮ` (проверить флаг `i` и паттерн `подписано[\s\S]{0,80}подписью`), последние 30% текста
- [ ] 1.3 Интегрировать фильтр перед `renderLawText` в `fetch` handler: `filterLawText(data.text)` → `renderLawText(filtered)`, дата подписания (`extractSigningDate`) остаётся сверху; добавить фолбэк — если после фильтра < 20 символов, показать исходный экранированный текст как есть
- [ ] 1.4 Проверить, что единый скролл/обычный текст после заголовка сохранён (без `max-height`), таблицы по-прежнему рендерятся

## 2. Проверка

- [ ] 2.1 `py_compile` изменённых модулей, `node --check` JS, ручная проверка WebApp с реальными законами (постановление Правительства от 31.08.2026, закон с `ПОДПИСАНО ЭЛЕКТРОННОЙ ПОДПИСЬЮ`)
- [ ] 2.2 Логическая проверка сценариев `specs/content-viewing` (шапка фильтруется, хвост фильтруется, таблицы и фолбэк не ломаются, ссылка на оригинал остаётся)
