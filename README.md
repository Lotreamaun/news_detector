# News Detector — Parser Telegram Bot

Персональный RSS-агрегатор с саммаризацией новостей. Бот позволяет добавлять RSS-ленты, автоматически проверяет новости, создает краткие саммари и отправляет их в Telegram.

## 🚀 Функциональность

### Первый релиз (MVP)
- ✅ Добавление/удаление RSS-лент через Telegram бота
- ✅ Автоматическая проверка новостей каждые 15 минут
- ✅ Саммаризация статей (140-280 символов)
- ✅ Отправка в Telegram с ссылкой на оригинал
- ✅ Отслеживание уже отправленных новостей

### В плане
- 🔍 Поиск первоисточника новости
- 📊 Фактчекинг (кросс-референс по разным источникам)
- 🤖 Создания драфта поста в нужном TOV с помощью LLM

## 🏗 Архитектура

```
Пользователь → [Telegram Bot] → [Ядро приложения] → [База данных]
                                     ↓
                [Сервисы: RSS Парсер, Саммаризатор, Планировщик]
```

### Поток данных:
1. Пользователь добавляет RSS-ленту через бота
2. Планировщик периодически проверяет все ленты
3. RSS Парсер загружает новые статьи
4. Саммаризатор создает краткое содержание
5. Бот отправляет саммари пользователю
6. Все данные сохраняются в БД для отслеживания

## 📁 Структура проекта

```
news_detector/
├── .env                    # Конфиденциальные данные (НЕ КОММИТИТЬ!)
├── .gitignore             # Игнорируемые файлы
├── requirements.txt       # Зависимости Python
├── README.md              # Эта документация
│
└── app/                   # Основное приложение
    ├── __init__.py
    ├── main.py            # Точка входа в приложение
    │
    ├── bot/               # Telegram-бот
    │   ├── __init__.py
    │   └── handlers.py    # Обработчики команд и клавиатуры
    │
    ├── core/              # Ядро приложения
    │   ├── __init__.py
    │   ├── config.py      # Конфигурация и настройки
    │   └── database.py     # Подключение к базе данных
    │
    ├── models/             # Модели базы данных
    │   ├── __init__.py
    │   ├── user.py        # Пользователи бота
    │   ├── feed.py        # RSS-ленты пользователей
    │   └── article.py     # Новости
    │
    └── services/          # Бизнес-логика
        ├── __init__.py
        ├── rss_parser.py  # Парсинг RSS и статей
        ├── summarizer.py  # Создание саммари
        └── scheduler.py   # Планировщик задач
```

## 🧩 Модули и ответственность

### 1. **app/main.py** - Точка входа
- Запуск всего приложения
- Инициализация всех компонентов
- Обработка graceful shutdown

### 2. **app/bot/handlers.py** - Telegram интерфейс
- Обработка команд (`/start`, `/feeds`, `/add_feed`, `delete_feed`, `/help`)
- Создание клавиатур и кнопок
- Диалог с пользователем (добавление RSS)
- Отправка уведомлений о новых новостях

### 3. **app/core/config.py** - Конфигурация
- Загрузка переменных окружения из `.env`
- Централизованное хранение настроек
- Валидация обязательных параметров

### 4. **app/core/database.py** - Работа с БД
- Подключение к базе данных
- Создание сессий для работы с БД
- Инициализация таблиц

### 5. **app/models/** - Структура данных
- **user.py**: Пользователи Telegram (id, username, подписки)
- **feed.py**: RSS-ленты (url, название, владелец, активность)
- **article.py**: Новостные статьи (заголовок, текст, саммари, дата)

### 6. **app/services/rss_parser.py** - Парсер статей
- Загрузка и парсинг RSS-ленты
- Извлечение полного текста статьи
- Определение свежих новостей

### 7. **app/services/summarizer.py** - Анализ текста
- Создание краткого саммари (140-280 символов)
- Экстрактивное суммирование (без LLM)
- Обрезка и форматирование текста

### 8. **app/services/scheduler.py** - Планирование
- Периодическая проверка RSS-лент
- Управление интервалами проверки
- Координация задач парсинга

## 🛠 Технологический стек

### Основные:
- **Python 3.14+** - основной язык
- **python-telegram-bot 22+** - асинхронный фреймворк для бота
- **SQLAlchemy 2.0** - ORM для работы с БД
- **SQLite** - база данных (легкая, в файле)

### Парсинг:
- **feedparser** - парсинг RSS/Atom лент
- **trafilatura** - извлечение текста из веб-страниц
- **sumy** - создание саммари текста

### Вспомогательные:
- **python-dotenv** - управление переменными окружения
- **aiohttp** - асинхронные HTTP-запросы

## ⚙️ Запуск

#TODO: Добавить инструкцию по запуску на локалхосте

## 🔧 Разработка

### Добавление новой команды в бота:
1. Добавить обработчик в `app/bot/handlers.py`
2. Зарегистрировать обработчик в `app/main.py`
3. При необходимости добавить клавиатуру

### Добавление новой модели:
1. Создать файл в `app/models/`
2. Наследоваться от `Base` из `app.core.database`
3. Импортировать модель в `app/core/database.py` для создания таблиц

### Добавление нового сервиса:
1. Создать файл в `app/services/`
2. Реализовать бизнес-логику
3. Импортировать и использовать в основном потоке

## 🐛 Отладка

### Логирование:
Логи пишутся в консоль и в файл `bot.log`:
```bash
# Просмотр логов в реальном времени
tail -f bot.log
```